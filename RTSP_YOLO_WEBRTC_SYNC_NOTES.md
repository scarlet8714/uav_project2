# RTSP、YOLO 與 WebRTC/Canvas 架構討論筆記

## 目標

攝影機以 RTSP 輸入，瀏覽器播放影片，YOLO 負責推論，HTML Canvas 疊加偵測框。希望降低不必要的 memory copy、避免影片被推論卡住，並讓框與影格同步。

## 建議的基本架構

```text
RTSP（H.264/H.265 壓縮串流）
                  │
          接收一次、壓縮資料分流
          ┌───────┴────────┐
          ▼                ▼
   WebRTC/其他瀏覽器     YOLO 路徑
   可用的串流轉送        硬體解碼
   （不一定要解碼）      前處理 → 推論
          │                │
       <video>       timestamp + boxes
          │                │
          └── Canvas 疊加框 ┘
```

瀏覽器通常不能直接播放 RTSP，因此需要透過 WebRTC、HLS 或其他瀏覽器可用的協定。若來源編碼與瀏覽器相容，WebRTC 路徑可以只做封裝與傳輸，不必在 Jetson 先解碼再重新編碼。

## 兩種主要方案

### 方案 A：壓縮串流分流，只有 YOLO 解碼

```text
RTSP 壓縮資料
 ├─ WebRTC：轉送原本的編碼資料
 └─ Jetson：硬體解碼 → YOLO
```

優點：

- 不需要為了 WebRTC 多做一次解碼與編碼。
- Jetson 只為 YOLO 做必要的解碼。
- 不會因為畫框而複製整張影像；前端 Canvas 只處理框與文字。

代價與注意事項：

- 來源 H.264/H.265 必須符合瀏覽器與 WebRTC 的支援範圍。
- 需要處理來源 timestamp 與瀏覽器 `video.currentTime` 的時間軸對應。
- YOLO 推論慢時，必須用獨立 queue／buffer，避免阻塞影片轉送。

### 方案 B：解碼一次後分流，一路重新編碼

```text
RTSP → 解碼一次 → 原始影像分流
                    ├─ YOLO
                    └─ 重新編碼 → WebRTC
```

優點：

- YOLO 與 WebRTC 使用同一個解碼影格，PTS/timestamp 對應較直觀。
- 適合來源編碼不被瀏覽器支援，或需要嚴格控制輸出影格的情況。

代價：

- 增加編碼器負載、記憶體頻寬與 buffer 使用量。
- 會增加延遲，編碼設定不當時可能有更多緩衝。
- 可能造成畫質損失。
- 若使用 B-frame，影格顯示順序與編碼順序會更複雜；低延遲情境應優先使用低延遲設定。

因此，方案 B 不是同步的必要條件，而是用額外解碼後處理成本，換取比較容易管理的同步關係。

## 推論頻率

可以每兩幀推論一次。例如影片 30 FPS，YOLO 處理約 15 FPS，影片仍維持 30 FPS 播放。

- 每兩幀推論一次會降低推論與部分前處理成本。
- H.264/H.265 仍通常需要持續解碼，因為影格之間有參考關係；解碼成本不會直接減半。
- 沒有新推論結果的中間影格，可以暫時沿用上一個框。
- 若需要更平順，可在相鄰兩次結果之間插值，但要處理物件配對、出現與消失。

## Timestamp 與時間基準

「時間基準」是指影片影格與 YOLO 結果使用同一套影格識別方式，知道某一組框屬於哪一張影像。

例如來源影格：

```text
影格 A：timestamp = 10.000 秒
影格 B：timestamp = 10.033 秒
影格 C：timestamp = 10.066 秒
```

YOLO 處理影格 B 後，結果應包含：

```text
timestamp = 10.033 秒
boxes = [...]
```

前端播放到同一個媒體時間時，才繪製這組框。timestamp 優先使用攝影機／RTP 的 timestamp 或 pipeline 中的 PTS；影格編號也可用，但在丟幀或可變 FPS 時較不穩定。

Jetson 的 monotonic clock 適合用來計算收到影格時間、推論耗時和傳輸延遲，但不能直接拿系統目前時間與瀏覽器 `video.currentTime` 比較，因為兩者不是同一個時間軸。

## 影片延遲與同步

影片延遲的目的是留時間給 YOLO 完成推論和傳送結果。例如：

```text
影片目前實際收到：10.200 秒
前端目前播放：    10.100 秒
YOLO 結果：        10.100 秒
```

前端播放到 10.100 秒時，對應框已經到達，因此框可以同步顯示。延遲量應涵蓋推論時間、結果傳輸時間與抖動；不應盲目把所有時間重複相加。

瀏覽器的 `video.currentTime` 通常是 WebRTC 媒體串流自己的時間軸，不一定等於攝影機原始 RTP timestamp。因此需要在串流開始時建立一次映射，例如：

```text
來源 timestamp 10.100 秒 ↔ 瀏覽器 video.currentTime 3.420 秒
```

之後用相對時間維持這個偏移，或在後端／傳輸層讓 metadata 與媒體時間軸保持一致。

## Memory copy 的重點

- Canvas 只畫框時，傳送的通常只是少量座標、類別與信心值，不需要複製整張影像。
- 每幀把影片畫入 Canvas、呼叫 `getImageData()`、或把影像轉成 Base64，才容易增加大量像素搬移與 GPU/CPU 同步成本。
- Jetson 上應盡量維持硬體解碼與適合的 GPU/NVMM 路徑，避免不必要地把整張影像搬回 CPU。
- 1920×1080 RGBA 約 8.3 MB；完整複製 30 次／秒，單純資料量就約 249 MB/s。

## 後續討論時的決策順序

1. 確認攝影機編碼、profile、是否有 B-frame，以及瀏覽器能否直接接收。
2. 若相容，優先評估方案 A：壓縮資料轉送，YOLO 獨立解碼。
3. 定義來源 PTS/RTP timestamp、YOLO 結果格式與瀏覽器播放時間的映射。
4. 量測推論耗時與抖動，再決定影片延遲和 buffer 大小。
5. 若來源不相容或方案 A 的同步控制不足，再考慮方案 B：解碼一次、分流、重新編碼。

## 核心結論

「影片轉送」與「YOLO 解碼」可以是兩條邏輯路徑；不一定要因為 YOLO 已經解碼，就再把影像解碼後重新編碼給瀏覽器。只要來源編碼相容，方案 A 通常較省資源。解碼後再編碼的方案主要是為了瀏覽器相容性或更容易控制影格同步，而不是同步的唯一方法。
