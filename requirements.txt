# Jetson system Python 3.10 dependencies (Jetson Linux R36.3).
# Install this file first, then:
# python3 -m pip install --user --no-deps -r requirements-yolo.txt
# Ultralytics is installed separately to preserve the system-built OpenCV
# and NVIDIA PyTorch/torchvision. These must already be installed.
#
# GStreamer, PyGObject (gi), tiscamera/tcambin, Tcam introspection,
# v4l2-ctl, camera drivers, CUDA/TensorRT and Jetson-specific PyTorch
# builds are system dependencies and cannot be installed from this file.

aiohttp==3.14.3
aiortc==1.15.0
av==17.1.0
Flask==3.1.3
numpy==1.26.1
pynmea2==1.19.0
pyserial==3.5
# Ultralytics dependencies, excluding system OpenCV and NVIDIA torch/torchvision.
filelock>=3.16.1
matplotlib>=3.3.0
pillow>=7.1.2
pyyaml>=5.3.1
requests>=2.23.0
psutil>=5.8.0
polars>=0.20.0
nvidia-ml-py>=12.0.0
ultralytics-thop==2.1.6
